# 亲友团 · 2026-09-21 工作任务清单与修复记录

> 目的：为后续工作提供文档支持与排查依据。记录今日完成的修复、根因、验证结果、生产环境关键信息与遗留待办。
> 环境：Go 1.25 + Vue3 + SQLite 单体；生产 Oracle Cloud ARM64 `64.181.244.178`；面板 `https://myservs.rouroujuxuan.top/qyt/`。

---

## 1. 今日任务总览

| # | 任务 | 状态 | 结论/提交 |
|---|---|---|---|
| 1 | 订阅刷新 timeout/error（argo 节点全挂） | ✅ 已修复 | 回源入站未派生 + temporary 13 端口错配；`ea0f6fe` |
| 2 | 直连节点连不上（客户端 Clash） | ✅ 已修复 | vmess+Reality 链接缺参数 + Mihomo 不支持 vmess+Reality；`3a677f3` `cb89e2d` `f2ed0f4` |
| 3 | 20086 端口公网不通 | ✅ 已修复 | iptables 未放行，已放行并持久化 |
| 4 | `TestRFC5987Escape` 既有失败 | ✅ 已修复 | 期望值对齐品牌名"亲友团"；`178467b` |
| 5 | 订阅缓存 | ✅ 已确认无需改动 | `collectEntries` 已有 30s 链接缓存；响应体 `no-store` 为刻意设计 |
| 6 | 固定隧道（fixed）配置 | ⏳ 待办 | 见 §4，需 Cloudflare 控制台操作 |
| 7 | HTTPS 证书方案 | ⏳ 建议保持 LE | 见 §5 |
| 8 | logo/ico 重新设计 | ⏳ 待做 | 设计任务 |
| 9 | 前端科技简洁风美化 | ⏳ 待做 | `global.css` + 27 视图组件 |
| 10 | vue-tsc 类型错误清理 | ⏳ 待做 | 一批既有类型错误 |

---

## 2. 已修复问题明细（根因 → 修复 → 验证）

### 2.1 Argo 隧道回源失败（订阅里 argo 节点全 timeout/error）

- **现象**：cloudflared 日志满屏 `Unable to reach the origin service ... EOF`；面板 `curl https://<trycloudflare>/ws` 502。
- **根因**（两条叠加）：
  1. **回源目标错误**：`ArgoSpec.TargetPort = ib.ListenPort`，cloudflared 明文 HTTP 回源到 **Reality(TLS)** 入站，sing-box 只接受 TLS 握手 → 秒断 EOF。设计文档要求"为 argo 入站派生无 TLS 明文 ws 回源入站"，移植实现漏掉。
  2. **temporary 13 端口错配**：quick tunnel 只服务 443(80)，其余 12 个 CDN 端口必超时。
- **修复**（`ea0f6fe`）：
  - `internal/sbproc/cloudflared.go`：新增 `ArgoOriginPortDelta = 10000`。
  - `internal/store/singbox.go`：新增 `argoOriginInbound`，为 argo 入站派生无 TLS 明文 ws 回源入站（`127.0.0.1:<ListenPort+10000>`，同 users/path）；`argoSpec()` 回源指向派生端口；`argoVariantLinks` 按 `mode` 区分（temporary 只发 443，fixed 才 13 端口）。
  - `main.go` / `internal/api/argo_remote.go`：同步回源端口偏移。
- **验证**：sing-box config 新增 `30086` 回源入站；cloudflared 单元 `--url http://127.0.0.1:30086`；日志无 EOF；订阅只剩 1 个 argo-443 节点；sing-box 客户端经 argo-443 访问 gstatic 返回 **204**。

### 2.2 直连节点连不上（客户端 Clash/Mihomo）

- **现象**：刷新/重新导入订阅后直连节点仍不通；服务端 e2e 却 204。
- **排查链**：
  1. 本机 openssl 连 `64.181.244.178:20086` TLSv1.3 正常、返回微软证书 → **网络与服务端 Reality 正常**。
  2. 订阅 singbox 格式直连节点缺 `reality` 块 → 根因 A。
  3. Clash 格式直连节点缺 `reality-opts` → 根因 B。
  4. **根因 C（最终）**：Mihomo（Clash 内核）对 **vmess+Reality 组合支持不完整**，协议换成 vless 后兼容。
- **修复**（`3a677f3` `cb89e2d` `f2ed0f4`）：
  - `internal/singbox/link.go`：vmess 链接 JSON 补 `security=reality/pbk/sid`；argo 变体清空 Reality（CF 边缘终结 TLS）。
  - `internal/subconv/singbox.go`：vmess 渲染透传 `security=reality`，pbk/sid 改用 `tlsParam` 读取（vmess 链接的键在 JSON payload）。
  - `internal/subconv/clash.go`：vmess 分支补 `reality-opts`。
  - **生产入站协议 `vmess` → `vless`**（DB `UPDATE sb_inbounds SET type='vless'`）；`argoOriginInbound`/argo 变体/`sb_admin.go` 校验支持 vless。
- **验证**：sing-box config 20086/30086 均为 `type: vless`；clash 订阅直连节点 `type: vless` + `reality-opts`；vless+Reality 直连 e2e **204**。

### 2.3 20086 端口公网不通

- **根因**：服务器 iptables 仅放行 `80/8081/22`，20086 被兜底 REJECT。
- **修复**：`iptables -I INPUT 1 -p tcp --dport 20086 -j ACCEPT`，`iptables-save > /etc/iptables/rules.v4` 持久化。
- **验证**：`Test-NetConnection 64.181.244.178:20086` → True。

### 2.4 TestRFC5987Escape

- **根因**：期望值还是旧品牌"轻舟"的 UTF-8 编码。
- **修复**：改为"亲友团" `%E4%BA%B2%E5%8F%8B%E5%9B%A2`（`178467b`）。

---

## 3. 生产环境关键信息（排查用）

| 项 | 值 |
|---|---|
| 服务器 | Oracle Cloud ARM64 `64.181.244.178`，SSH key `C:\Users\11304\oracle\ssh-key-2026-09-20.key` |
| 面板二进制 | `/home/ubuntu/qinyoutuan`（root 运行，端口 8081）；备份 `qinyoutuan.bak.*` |
| 数据库 | `/home/ubuntu/qinyoutuan.db`（WAL）；备份 `qinyoutuan.db.bak.*` |
| 主密钥 | `/home/ubuntu/qz-secret`（QZ_SECRET_KEY，勿丢） |
| sing-box | `/etc/sing-box/config.json`，入站：20086(vless+Reality+ws) / 30086(127.0.0.1 vless+ws 回源) |
| cloudflared | 单元 `cloudflared-<tag>.service`，回源 `http://127.0.0.1:30086`；日志 `/etc/cloudflared/argo-test.log` |
| nginx | 反代 `/qyt` → 8081；Let's Encrypt 自动续期 |
| 订阅 token | 用户在面板"更换订阅地址"后已变更；admin 当前 token 从 DB 读：`sqlite3 qinyoutuan.db "select sub_token from users where username='admin'"` |
| 防火墙 | iptables 已放行 22/80/443/8081/**20086**（持久化）；Oracle NSG 需控制台确认 |

### 排查命令速查

```bash
# 面板健康（本机）
curl -k https://127.0.0.1/qyt/api/health -H 'Host: myservs.rouroujuxuan.top'

# 看回源日志是否 EOF
sudo tail -n 20 /etc/cloudflared/argo-test.log | grep -c EOF

# 当前隧道域名
sudo grep -oE 'https://[a-z0-9-]+\.trycloudflare\.com' /etc/cloudflared/argo-test.log | tail -1

# 端口监听
sudo ss -tln | grep -E '20086|30086|8081|18080'

# 订阅内容（直连 8081 绕过 nginx）
curl -s http://127.0.0.1:8081/sub/<token>?format=singbox

# 公网端口探测（本机）
Test-NetConnection 64.181.244.178 -Port 20086
```

---

## 4. 固定隧道（fixed）配置方案

**目标**：13 端口全通 + 稳定域名 + 隐藏真实 IP。

**token 获取**（Cloudflare 控制台）：
1. 注册/登录 dash.cloudflare.com → 添加域名 `myservs.rouroujuxuan.top`（DNS 托管给 CF：aliyun 解析里把 NS 改为 CF 提供的两个 NS）。
2. **Zero Trust → Networks → Tunnels → Create a tunnel**（Named tunnel）→ 创建后复制 **token**（`eyJ...` JWT）。
3. 该 Tunnel 配置 **Public Hostname**：`sub.myservs.rouroujuxuan.top` → Service `http://127.0.0.1:30086`（**必须 30086 回源端口**，非 20086）。
4. 面板"入站 → Argo 隧道"：模式选**固定**，填 token + 域名 `sub.myservs.rouroujuxuan.top`。

> 语义：`temporary` = 1 端口(443)，`fixed` = 13 端口（代码红线）。

---

## 5. HTTPS 证书方案

- **现状**：Let's Encrypt HTTP-01 + certbot 自动续期 timer —— 已满足"到期自动切换"，零人工。
- **aliyun 免费证书**：DV 证书仅手动签发、不支持 ACME 自动续期；自动化需脚本调阿里云 API（申请→下载→替换 nginx→reload）。
- **建议**：保持 Let's Encrypt（80 端口已开放，HTTP-01 可用）。确需 aliyun 证书再另写自动脚本。

---

## 6. 遗留待办（按优先级）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | 固定隧道实机联调 | 配好 token 后 13 端口才成立（§4） |
| 高 | 多 IP 存活兜底订阅 | 移植项②，后端+前端完成（§10），待真实第二公网 IP 联调 |
| 中 | 纯 argo 模式（可选） | 关闭直连只发 argo，隐藏真实 IP；用户暂缓 |
| 中 | 前端 vue-tsc 类型错误 | 一批既有错误，工作量大 |
| 中 | logo/ico 重新设计 | 设计任务 |
| 中 | 前端科技简洁风美化 | `global.css` + 27 视图组件 |
| 低 | 本机 Docker 构建镜像源 | 曾 403/525 |

---

## 7. 本次相关提交（dev 分支）

| 提交 | 说明 |
|---|---|
| `ea0f6fe` | fix(argo): 派生无 TLS 明文 ws 回源入站并限制临时隧道只发 443 |
| `064d541` | docs: HANDOFF 标记 6.3-1/6.3-2 已修复 |
| `3a677f3` | fix(sub): vmess+Reality 订阅链接补全 pbk/sid，argo 变体清空 Reality |
| `0dc1882` | docs: HANDOFF 记录 vmess+Reality 修复 |
| `cb89e2d` | fix(sub): clash 格式补 vmess+Reality reality-opts |
| `178467b` | fix(test): TestRFC5987Escape 期望值对齐"亲友团" |
| `f2ed0f4` | feat(argo): 支持 vless(ws) 入站挂 Argo 隧道（生产入站已改 vless） |

> 后续补充提交见 §9「06-22 增补」。

---

## 8. 2026-09-22 增补：UI 重设计 + 深色模式

> 在 09-21 基础上继续完成前端 UI 与主题工作，并记录直连节点最终修复。

### 8.1 直连节点最终修复（客户端 Clash/Mihomo）
- **现象**：即使订阅独占完整 Reality 参数、服务端 `vless+Reality+ws` e2e 通，Clash 仍连不上。
- **最终根因**：Mihomo（Clash 内核）对 **vmess+Reality**、以及 **Reality + WebSocket 传输** 这类组合支持不完整（Reality 标准用法是 **raw TCP**）。
- **修复**：
  1. 生产入站 20086 清空 options → **vless+Reality+raw TCP**（`transport` 为空）。
  2. 30086 回源入站仍为 `vless+ws`（cloudflared 回源需要 ws）。
  3. `internal/store/singbox.go`：`argoVariantLinks` 强制 argo 变体 `network=ws`、无 RawTCP 时补 `path=/ws`（订阅变体与回源入站 path 必须一致）。
- **结果**：Clash 订阅直连节点为标准 `vless + network:tcp + reality-opts`，**e2e 204**，用户确认真机可通。argo-443 保持 ws 不受影响。
- **验证**：op=直连 20086 e2e 204；clash/singbox/base64 三种订阅格式 Reality 参数齐全。
- **提交**：`1de8fb1`（argo 变体强制 ws path）、`f2ed0f4`（vless argo）、`cb89e2d`（clash reality-opts）、`3a677f3`（vmess reality 链接）。

### 8.2 Logo 重新设计（科技简洁风）
- 重写 `frontend/public/qinyoutuan-mark.svg`：深空蓝圆角底 + 雷达弧 + 中心渐变核心 + 环绕节点（青→蓝→紫霓虹），表意"团聚/连接/网络"。
- favicon 用此 SVG（现代标准，矢量清晰），`BrandMark.vue` 自动跟随并加科技蓝光晕。

### 8.3 前端科技简洁风美化
- `global.css` 主题层整体升级：品牌色 `#1769a5` → 科技蓝紫 `#3b6cf0`；冷调背景 `#eef2f8` + 青/紫光晕；主按钮青蓝紫渐变 + 光晕悬浮；卡片 hover 浮现品牌渐变顶条；文字/边框/阴影改 slate 系。
- `App.vue` `themeOverrides`（无界组件色）同步为新品牌色。

### 8.4 深色模式（跟随系统 + 手动切换）
- **机制**：新增 `frontend/src/stores/theme.ts`，默认跟随系统（`prefers-color-scheme`），顶栏太阳/月亮按钮可手动覆盖，`localStorage('qz-theme')` 记忆。
- **CSS 层**：`global.css` 全部语义变量下沉为 `:root.dark` 深色套；集中 `.dark` 覆盖硬编码浅色组件（输入框/表格/单选组/标签/滚动条/Markdown/顶栏/sticky 底栏等）。
- **naive-ui 层**：`App.vue` 用 `NConfigProvider :theme`（`darkTheme/lightTheme`）+ 深浅两套 `themeOverrides`，系统切换实时跟随。

### 8.5 登录页科技定制
- `LoginDialog.vue`：背景改主题变量（深色适配），顶部品牌渐变光带 + 青蓝紫科技光晕。

### 8.6 范围与进度
- 控制台/订阅页先经全局科技组件（n-card / stat-card / list-card 均已深色适配）达成统一；**2026-09-22 起已逐步对全部 28 个视图组件逐个精修**硬编码浅色（改为语义变量 / 加 `:global(.dark)` 覆盖），提交 `0761304` 后已构建部署。

---

## 9. 2026-09-22 后续提交补充

| 提交 | 说明 |
|---|---|
| `b008655` | docs: 新增 2026-09-21 工作任务清单与修复记录（本文件） |
| `1de8fb1` | fix(argo): 直连 raw TCP Reality 时 argo 变体强制 ws path=/ws |
| `2c29bf5` | style: 新 logo + 科技简洁风主题（蓝紫品牌、冷调背景、卡片顶条） |
| `1911d47` | feat(ui): 深色模式（跟随系统+手动切换）+ 登录页科技定制 |

## 10. 当前任务进度表（2026-09-22 更新）

| # | 任务 | 状态 | 说明 |
|---|---|---|---|
| 直连 | Clash 无法连接 | ✅ 完成 | root cause=Reality 需 raw TCP；生产改 vless+TCP；e2e 204 |
| logo | 重新设计 logo/favicon | ✅ 完成 | 新 SVG 科技风；`2c29bf5` |
| 前端美化 | 科技简洁风 | ✅ 完成 | 全局主题 + 主按钮/卡片/背景；`2c29bf5` |
| 深色模式 | 跟随系统+手动切换 | ✅ 完成 | 基础设施+顶栏切换；`1911d47` |
| 登录页 | 科技定制版式 | ✅ 完成 | 渐变顶条+光晕；`1911d47` |
| 控制台/订阅 | 科技定制 | ✅ 完成 | 逐页专属版式：控制台=品牌渐变+光晕头部（问候/状态徽章）+用量环渐变大字+发光弧；订阅=品牌渐变摘要信息带+专属链路卡（渐变顶条/光晕/等宽链接字）。复用全局 CSS 变量，深色自动适配 |
| 深色审计 | 27 视图组件 | ✅ 完成 | 逐个精修硬编码浅色→主题变量/加 `.dark` 覆盖（`0761304`）；含 Monitor/AdminMonitor/AdminSingbox/AdminNodes/用户订单/积分/商城等 |
| 固定隧道 | fixed 实机联调 | ⏳ 待办 | 需 CF 控制台 token |
| 多 IP 兜底 | 移植项② | ◐ 后端+前端完成，真实第二公网 IP 联调待办 | 后端：`sb_inbounds.fallback_hosts` 列(迁移) + liveness 探测选主 + BuildSelfBuiltLinks 备选变体（`ac4ec17`）；前端：入站表单「备选 IP/域名」标签编辑（AdminSingbox）+ 用户「我的订阅」经 BuildSelfBuiltLinks 自动含 `-ipN` 变体，无需改 UserSub |
| 全量测试 | go test ./... 含 race | ✅ 完成 | 服务器(Linux 有 systemd) `go test -race -count=1 ./...` exit=0 全绿；本机 Windows 单例失败（sbctl 需 systemd）为环境相关非回归；服务器已装 build-essential 供 race 用 cgo |
| vue-tsc | 类型错误清理 | ✅ 完成 | vue-tsc 5.7.3 实测 0 错误（README 所述既有错误已在历次重构消除） |

---

## 11. 2026-09-22 运维：开机自启 + 看门狗 + 官网

> 服务器重启后暴露出问题（面板以 nohup 手动启动，无 systemd，重启不自启）。本次补齐开机自启、看门狗告警与官网。运维脚本已归档 `deploy/ops/`。

### 11.1 开机自启现状（诊断结论）
| 服务 | 诊断 | 结论 |
|---|---|---|
| nginx | `systemctl is-enabled` = enabled | ✅ 已有开机自启 |
| sing-box | enabled | ✅ 开机自启（负责 20086 直连 + 30086 回源） |
| cloudflared | systemd 单元存在（`cloudflared-<tag>.service`） | ✅ 隧道 |
| **亲友团面板** | **无 systemd 单元**，原本靠 nohup 手动启动 | ⚠️ **重启不自启（本次已修复）** |

### 11.2 亲友团面板 systemd 单元（新增开机自启 + 崩溃自愈）
- 新增 `/etc/systemd/system/qinyoutuan.service`，`Restart=always`、`RestartSec=5`、`StartLimitIntervalSec=30`/`StartLimitBurst=5`（防死循环激荡）。
- 环境文件 `/etc/qinyoutuan.env`（含 `QZ_SECRET_KEY`、`QZ_PUBLIC_BASE` 等，权限 600）。
- 已 `enable + start`：`active + enabled + health 200`。
- 脚本：`deploy/ops/deploy_panel_unit.sh`。

### 11.3 看门狗（每小时检测 + 飞书 webhook 通知）
- 脚本 `/usr/local/bin/qz-watchdog.sh`：检测 `nginx`、`qinyoutuan`，未运行 → 发飞书「服务中止」→ `systemctl restart` → 成功发「服务已自动启动」/ 失败发「重启失败」。消息格式：
  - `服务器异常：xxx 服务中止，请关注 IP：64.181.244.178, 时间：YYYY-MM-DD HH:MM:SS`
  - `xxx 服务已自动启动，请关注 IP：64.181.244.178, 时间：…`
- systemd `qz-watchdog.timer`：`OnCalendar=hourly`（每整点）+ `Persistent=true`，service 已 `enable --now`。
- 日志 `/var/log/qz-watchdog.log`。
- 脚本/安装：`deploy/ops/qz-watchdog.sh`、`deploy/ops/install_watchdog.sh`。

### 11.4 官网 rouroujuxuan.top
- 域名 `rouroujuxuan.top` 解析到 **Cloudflare IP**（104.21.12.99）→ 全程 CF 代理。访客与 CF 之间用 CF 自身证书；源站只需回源证书。
- 方案：**自签回源证书**（openssl 10 年，SAN 含 www），nginx 443 用；**80 也直接 serve**（兼容 CF Flexible 80 回源，避免 301 自循环——曾遇 CF 返回同址 301 死循环）。
- nginx vhost `/etc/nginx/sites-available/rouroujuxuan`（软链 enabled），root `/var/www/rouroujuxuan`。
- 页面：科技风静态单页（`website_index.html`）。
- **验证**：公网 `http(s)://rouroujuxuan.top/` 均 **200**，标题「亲友团 · 安全连接服务」。
- 脚本：`deploy/ops/install_website.sh`、`website_index.html`。

> ⚠️ 官网证书为**自签回源证书**（非权威 CA 签发给 `rouroujuxuan.top`）：访客浏览器不发警告，因为域名经 Cloudflare 代理、访客看到的是 CF 边缘的有效证书；但**直连源站 IP:443 时**会出现自签警告。如需对源站也权威可信，可在 CF 后台下载 **Origin Certificate** 替换，或把该域名改为 DNS-Only（灰云）后用 certbot（Let's Encrypt）签发。

### 11.5 官网证书升级为 CF Origin Certificate（2026-09-22）
- 目的：配合 Cloudflare 把 `rouroujuxuan.top` 的 SSL 加密模式从「灵活」切换到「完整（严格）」（strict 拒绝自签）。
- 已用 CF 后台生成的 **Origin Certificate** 替换 `/etc/nginx/ssl/rouroujuxuan.top/{fullchain,privkey}.pem`：
  - 证书链：`CloudFlare Origin Certificate`（issuer `CloudFlare Origin SSL Certificate Authority`）
  - 有效期：`2026-09-22` → `2041-09-18`（15 年）
  - 证书/私钥 modulus 校验 **MATCH OK**，nginx -t 通过，公网 `http/https://rouroujuxuan.top` 均 200。
- **状态**：服务器端已就绪，可在 CF 后台将加密模式切换为「完整（严格）」。面板站 `myservs` 为 Let's Encrypt 权威证书，切换不受影响。
- ⚠️ 私钥不入 git：证书部署仅存于服务器 `/etc/nginx/ssl/rouroujuxuan.top/`，如需在 CF 后台再生成请自行妥善保管。

### 11.6 新增一级子域 qyt.rouroujuxuan.top（2026-09-22）
- 需求：`https://qyt.rouroujuxuan.top` → 反代后端 `192.236.223.225:8086`（HTTP），80 强制跳 443。
- 实现：新增 `deploy/ops/add_subdomain_qyt.sh` → nginx vhost `/etc/nginx/sites-available/qyt-rourou`（软链 enabled）：
  - `listen 80` `server_name qyt.rouroujuxuan.top` → `return 301 https://$host$request_uri`
  - `listen 443 ssl` 复用 Origin **通配证书**（`*.rouroujuxuan.top` 已覆盖该一级子域，无需新证书）→ `proxy_pass http://192.236.223.225:8086`（含 Upgrade/Connection 头，支持 WebSocket）
- 验证：nginx -t 通过、reload 生效、`nginx -T` 确认加载；**本机走 443 反代到后端返回 200**（后端 192.236.223.225:8086 可达）。
- ⚠️ **待办（需用户在 Cloudflare 完成）**：公网 `https://qyt.rouroujuxuan.top` 当前无法解析（code 000），需在 CF 后台给该子域加 **A 记录 `qyt` → `64.181.244.178`，并开启代理（橙云）**。因使用 Origin 通配证书且它仅被 Cloudflare 信任，**必须走 CF 代理**（橙云）否则直连会证书告警。配置后即可公网访问。
- **（2026-09-22 后续）已就绪，最终根因与修复**：
  - CF 已配 `A qyt → 64.181.244.178` 并代理（橙云），公网 DNS（8.8.8.8/1.1.1.1）解析到 CF IP，记录生效；源站证书 SAN `*.rouroujuxuan.top, rouroujuxuan.top` 覆盖 `qyt`。
  - **真正根因**：切到 Full(strict) 后 CF 改用 **HTTPS(443) 回源**，但服务器 **iptables 未放行 443 入站**（只放行 80/20086/8081/22，其被末尾 `REJECT all` 挡掉）→ CF 回源全站 `523`（官网、qyt、面板若走 CF 都受影响）。此前 CF 用 Flexible(80) 回源，80 已放行所以表面正常。
  - **修复**：`iptables -I INPUT 1 -p tcp --dport 443 -j ACCEPT` 并 `iptables-save > /etc/iptables/rules.v4` 持久化。放行后 **qyt / 官网经 CF 回源均 200**（本机及外部实测）。
  - 用户侧 `DNS_PROBE_FINISHED_NXDOMAIN`：本机/运营商 DNS 对旧 NXDOMAIN 负缓存，执行 `ipconfig /flushdns`，若仍不行把本机 DNS 临时改为公共 `1.1.1.1`/`8.8.8.8`，或等待负缓存过期。
