# 保持构建上下文精简；被覆盖/重建或与镜像无关的都排除。
.git
.github
*.md
docs/

# 前端依赖与产物（镜像内 npm ci + vite build 重新生成）
frontend/node_modules
frontend/dist

# 探针预编译产物（镜像内重新交叉编译两架构）
cmd/probe/dist

# 本地数据与构建产物
*.db
*.db-wal
*.db-shm
out/
qinyoutuan
qinyoutuan.exe

# 编辑器/杂项
.vscode
.idea
.DS_Store
