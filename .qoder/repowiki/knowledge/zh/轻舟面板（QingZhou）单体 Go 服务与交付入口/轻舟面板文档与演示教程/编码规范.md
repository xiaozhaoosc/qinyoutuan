- 录屏脚本采用统一的表格结构描述每个场景：截图文件名、画面、操作步骤、口播稿、屏幕字幕、时长，便于逐条录制。
- 截图文件命名遵循 `<角色前缀>-<页面>.png` 约定（如 `pub-home`、`user-dashboard`、`admin-servers`），并在 README 中集中列出清单。
- 管理员文档以 Markdown 章节+表格形式呈现安装步骤、环境变量与协议选型等决策表，关键命令用代码块包裹以便复制。
- HTML 教程与 Markdown 脚本通过相对路径引用 `截图/` 目录中的 PNG，保持素材与教程同目录发布。
