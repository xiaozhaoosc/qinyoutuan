开发模式：在 `frontend/` 下执行 `npm run dev`，Vite 默认监听 5173 端口并将 `/api`、`/sub` 请求代理到本地后端 `http://127.0.0.1:8081`；生产构建需先运行 `npm run build` 生成 `dist/`，再重新编译 Go 二进制以嵌入前端资源，或设置 `QZ_WEB_DIR=frontend/dist` 让 Go 进程直接读取磁盘目录。
