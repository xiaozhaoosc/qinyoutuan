- 所有运行时配置优先从 QZ_* 环境变量读取，其次回退到 store.Setting 表中的同名键，最后取默认值（如 QZ_SMTP_HOST → smtp_host → 空则禁用）
- 敏感配置（SMTP 密码、sing-box TLS/Reality 密钥）在数据库中通过 QZ_SECRET_KEY 加密存储，未设置时降级为 jwt_secret 并输出告警日志
- 后台任务统一通过 app.StartXxx(ctx, interval) 形式启动，并使用 sync.WaitGroup 在 shutdown 阶段等待写事务完成后再关闭 DB
