Go 1.25 + chi/v5 HTTP 路由 + modernc.org/sqlite 嵌入式数据库（CGO=0）+ golang-jwt/jwt v5 + golang.org/x/crypto；前端基于 Vue 3 + Vite + Naive UI，产物通过 go:embed 内嵌到 Go 二进制中；容器化使用 docker buildx 多架构构建（linux/amd64, linux/arm64）。
