docker build --build-arg VERSION=v0.2.7 -t qingzhou . 进行本地多阶段构建；docker compose up -d 按 docker-compose.yml 快速拉起，首次运行若未设置 QZ_ADMIN_PASS 会打印随机管理员密码；生产环境可通过 install.sh 安装 systemd 单元并由 deploy 模块提供的脚本完成升级/备份。
