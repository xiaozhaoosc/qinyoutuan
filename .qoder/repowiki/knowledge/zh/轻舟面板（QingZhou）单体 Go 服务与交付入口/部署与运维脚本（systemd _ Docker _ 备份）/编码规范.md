- 所有外部可写路径集中在 `/opt/qingzhou`，systemd 通过 `ProtectSystem=full` 强制只读 `/usr`，避免服务进程越权写入系统目录。
- 敏感配置（如 `QZ_SECRET_KEY`、SMTP 密码）以环境变量形式传入并通过 `EnvironmentFile` 加载，禁止提交真实值，示例文件统一以注释形式列出键名。
- 运维脚本采用 POSIX shell + `set -e` 风格，输出带时间戳日志，并通过 `find ... -mtime +N -delete` 实现固定天数轮转清理。
