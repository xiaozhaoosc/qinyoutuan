生产部署需将 `qingzhou.env.example` 复制为 `/opt/qingzhou/qingzhou.env` 并 `chmod 600`；数据库位于 `/opt/qingzhou/qingzhou.db`，备份目录 `/opt/qingzhou/backups/`；可通过 cron 每天 04:30 执行 `backup.sh` 保留 7 天；在线更新要求二进制通过 `-ldflags "-X qingzhou/internal/version.Version=<tag>"` 注入版本。
